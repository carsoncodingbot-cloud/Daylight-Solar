# Daylight Solar — daylightsolar.ai

Production deployment of the Daylight Solar site (built static export in `site/`,
auto-deployed to GitHub Pages on every push to main).

Source code lives in `carsoncodingbot-cloud/Brand-building-skills` under `solar-site/`.
To update: rebuild there (`EXPORT=true npm run build`), copy `out/` here into `site/`, push.
